#include <stdio.h>
/**
 * main -Start point
 * Description: 'Prints alphabets in lowercase followed by a new line'
 * Return: Always 0
 */
int main(void)

{
	char la;

	for (la = 'a'; la <= 'z'; la++)
	{
		if
			(la != 'e' && la != 'q');
	}
	putchar('\n');

	return (0);
}
